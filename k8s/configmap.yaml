apiVersion: v1
kind: ConfigMap
metadata:
  name: fourinrow-config
  namespace: fourinrow
data:
  PORT: "5000"
  NODE_ENV: "production"
  KAFKA_ENABLED: "true"
  MATCHMAKING_TIMEOUT: "10000"
  RECONNECTION_TIMEOUT: "30000"
  # Database connection will use the postgres service
  DATABASE_URL: "postgresql://postgres:postgres@postgres-service:5432/fourinrow"
  # Kafka connection will use the kafka service
  KAFKA_BROKER: "kafka-service:9092"
